import createTheme, { ThemeContext, ThemeProvider } from './createTheme'
import useTheme from './useTheme'
import withTheme from './withTheme'

export default {
  ThemeContext,
  ThemeProvider,
  createTheme,
  useTheme,
  withTheme,
}
